#ifndef MECH_HPP
#define MECH_HPP

class Mech
{
	/*
	private:
		Role role;
		Weapon wep;
		Ability ab1;
		int health;
		int speed;
		int weight;
	public:
		Mech(Role r, Weapon w, Ability a, int h, int s, int w);

		Role getRole();
		Weapon getWeapon();
		Ability getAbility();
		int getHealth();
	*/
};

#endif